{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "corepack enable && corepack prepare pnpm@9.15.9 --activate && pnpm -C apps/ui install --frozen-lockfile",
  "buildCommand": "corepack enable && corepack prepare pnpm@9.15.9 --activate && pnpm -C apps/ui build",
  "outputDirectory": "apps/ui/dist"
}
